diff --git a/.github/actions/setup-node-pnpm/action.yml b/.github/actions/setup-node-pnpm/action.yml
deleted file mode 100644
index 745a86d5..00000000
--- a/.github/actions/setup-node-pnpm/action.yml
+++ /dev/null
@@ -1,34 +0,0 @@
-name: "Setup Node.js and pnpm"
-description: "Consolidated action to setup Node.js, pnpm, and check runtime"
-inputs:
-  node-version-file:
-    description: "Path to .node-version file"
-    required: false
-    default: ".node-version"
-  pnpm-version:
-    description: "Version of pnpm to activate"
-    required: false
-    default: "10.28.2"
-
-runs:
-  using: "composite"
-  steps:
-    - name: Setup pnpm
-      uses: pnpm/action-setup@v6
-
-    - name: Setup Node.js
-      uses: actions/setup-node@v6
-      with:
-        cache: 'pnpm'
-        cache-dependency-path: pnpm-lock.yaml
-        node-version-file: ${{ inputs.node-version-file }}
-
-    - name: Enable Corepack
-      run: corepack enable
-      shell: bash
-
-    - name: Activate pinned pnpm
-      env:
-        PNPM_VERSION: ${{ inputs.pnpm-version }}
-      run: corepack prepare pnpm@"$PNPM_VERSION" --activate
-      shell: bash
diff --git a/.github/actions/setup-workspace/action.yml b/.github/actions/setup-workspace/action.yml
index 0448c8af..1ab69071 100644
--- a/.github/actions/setup-workspace/action.yml
+++ b/.github/actions/setup-workspace/action.yml
@@ -1,9 +1,52 @@
 name: 'Setup Workspace'
-description: 'Bootstraps the workspace by installing dev-tools and configuring the environment'
+description: 'Bootstraps the workspace by installing Node.js, Python, and dev-tools'
+inputs:
+  setup-node:
+    description: "Install Node.js and pnpm"
+    required: false
+    default: 'true'
+  setup-python:
+    description: "Install dev-tools (Python)"
+    required: false
+    default: 'true'
+  node-version-file:
+    description: "Path to .node-version file"
+    required: false
+    default: ".node-version"
+  pnpm-version:
+    description: "Version of pnpm to activate"
+    required: false
+    default: "10.28.2"
+
 runs:
   using: 'composite'
   steps:
+    - name: Setup pnpm
+      if: inputs.setup-node == 'true'
+      uses: pnpm/action-setup@v6
+
+    - name: Setup Node.js
+      if: inputs.setup-node == 'true'
+      uses: actions/setup-node@v6
+      with:
+        cache: 'pnpm'
+        cache-dependency-path: pnpm-lock.yaml
+        node-version-file: ${{ inputs.node-version-file }}
+
+    - name: Enable Corepack
+      if: inputs.setup-node == 'true'
+      run: corepack enable
+      shell: bash
+
+    - name: Activate pinned pnpm
+      if: inputs.setup-node == 'true'
+      env:
+        PNPM_VERSION: ${{ inputs.pnpm-version }}
+      run: corepack prepare pnpm@"$PNPM_VERSION" --activate
+      shell: bash
+
     - name: Install dev-tools
+      if: inputs.setup-python == 'true'
       shell: bash
       run: |
         echo "::group::Install dev-tools"
@@ -16,6 +59,7 @@ runs:
         echo "::endgroup::"

     - name: Verify td-cli on PATH
+      if: inputs.setup-python == 'true'
       shell: bash
       run: |
         # Force a refresh of the path in the current shell context if possible,
@@ -35,6 +79,7 @@ runs:
         echo "✅ td-cli is resolvable on PATH (or confirmed available in local bin)."

     - name: Export Environment Variables
+      if: inputs.setup-python == 'true'
       shell: bash
       run: |
         # Use $(pwd) to get the container-internal path, avoiding host-path leakage from $GITHUB_WORKSPACE
diff --git a/.github/workflows/auto-conflict-resolver.yml b/.github/workflows/auto-conflict-resolver.yml
index 8509ca8a..5ffe7df4 100644
--- a/.github/workflows/auto-conflict-resolver.yml
+++ b/.github/workflows/auto-conflict-resolver.yml
@@ -97,8 +97,10 @@ jobs:
           fetch-depth: 0
           token: ${{ secrets.GITHUB_TOKEN }}

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
+      - name: Setup Workspace
+        uses: ./.github/actions/setup-workspace
+        with:
+          setup-python: 'false'

       - name: Check active runtime
         run: |
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index fc81d7f6..5a216324 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -85,9 +85,6 @@ jobs:
         with:
           fetch-depth: 0

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
-
       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace

@@ -112,9 +109,6 @@ jobs:
       # Setup dependencies once and use caching for speedup.
       - uses: actions/checkout@v7

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
-
       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace

@@ -172,9 +166,6 @@ jobs:
         with:
           fetch-depth: 0

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
-
       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace

@@ -266,9 +257,6 @@ jobs:
         with:
           fetch-depth: 0

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
-
       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace

diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index 78d6befa..ef12ce1c 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -37,8 +37,10 @@ jobs:
           fetch-depth: 0
           lfs: true

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
+      - name: Setup Workspace
+        uses: ./.github/actions/setup-workspace
+        with:
+          setup-python: 'false'

       - name: Check active runtime
         run: |
diff --git a/.github/workflows/jules-fix-trigger.yml b/.github/workflows/jules-fix-trigger.yml
index ff5934e1..f6d7520b 100644
--- a/.github/workflows/jules-fix-trigger.yml
+++ b/.github/workflows/jules-fix-trigger.yml
@@ -57,6 +57,8 @@ jobs:

       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace
+        with:
+          setup-node: 'false'

       - name: Initialize Jules Repair Session
         env:
diff --git a/.github/workflows/mergellama.yml b/.github/workflows/mergellama.yml
index 12c37fb4..d58029b3 100644
--- a/.github/workflows/mergellama.yml
+++ b/.github/workflows/mergellama.yml
@@ -34,9 +34,6 @@ jobs:
           fetch-depth: 0
           ref: ${{ github.head_ref }}

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
-
       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace

diff --git a/.github/workflows/security.yml b/.github/workflows/security.yml
index 06feaf57..91de7e8c 100644
--- a/.github/workflows/security.yml
+++ b/.github/workflows/security.yml
@@ -50,9 +50,6 @@ jobs:
         with:
           fetch-depth: 0

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
-
       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace

diff --git a/.github/workflows/update-snapshots.yml b/.github/workflows/update-snapshots.yml
index 58c34236..9e915f5b 100644
--- a/.github/workflows/update-snapshots.yml
+++ b/.github/workflows/update-snapshots.yml
@@ -59,8 +59,10 @@ jobs:
           token: ${{ secrets.GITHUB_TOKEN }}
           fetch-depth: 0

-      - name: Setup Node and pnpm
-        uses: ./.github/actions/setup-node-pnpm
+      - name: Setup Workspace
+        uses: ./.github/actions/setup-workspace
+        with:
+          setup-python: 'false'

       - name: Check active runtime
         run: |
diff --git a/.github/workflows/validate_issue.yml b/.github/workflows/validate_issue.yml
index a4195615..a0be1ba0 100644
--- a/.github/workflows/validate_issue.yml
+++ b/.github/workflows/validate_issue.yml
@@ -22,6 +22,8 @@ jobs:
           fetch-depth: 0
       - name: Setup Workspace
         uses: ./.github/actions/setup-workspace
+        with:
+          setup-node: 'false'

       - name: Validate Issue
         env:
diff --git a/boomtick-pkg/mcp/actions/setup/action.yml b/boomtick-pkg/mcp/actions/setup/action.yml
index cf5f7559..532aae52 100644
--- a/boomtick-pkg/mcp/actions/setup/action.yml
+++ b/boomtick-pkg/mcp/actions/setup/action.yml
@@ -3,16 +3,12 @@ description: Installs node/pnpm, workspace dependencies, td CLI and builds MCP s
 runs:
   using: composite
   steps:
-    - uses: ./.github/actions/setup-node-pnpm
+    - uses: ./.github/actions/setup-workspace

     - name: Install workspace dependencies
       run: pnpm install --frozen-lockfile
       shell: bash

-    - name: Install td CLI
-      run: pip install -e ./boomtick-pkg/cli --break-system-packages
-      shell: bash
-
     - name: Build MCP server
       run: pnpm --filter ./boomtick-pkg/mcp build
       shell: bash
diff --git a/docs/guides/upgrading-versions.md b/docs/guides/upgrading-versions.md
index 0d7a4cc4..a48fee8e 100644
--- a/docs/guides/upgrading-versions.md
+++ b/docs/guides/upgrading-versions.md
@@ -25,7 +25,7 @@ When upgrading pnpm (e.g. via dependabot), you must update the version in all of

 - `package.json` (in the `packageManager` field, and `engines.pnpm`)
 - `scripts/check-runtime-files.mjs` (in `expectedPnpm`)
-- `.github/actions/setup-node-pnpm/action.yml` (in the `pnpm-version` input default)
+- `.github/actions/setup-workspace/action.yml` (in the `pnpm-version` input default)
 - `.devcontainer/Dockerfile` (in the `PNPM_VERSION` environment variable)

 ## Upgrading node
