name: Prune Stale Previews
on:
  schedule:
    - cron: '0 */6 * * *' # Runs every 6 hours
  workflow_dispatch: # Allows manual triggering

concurrency:
  group: prune-stale-previews
  cancel-in-progress: false

jobs:
  call-workflow:
    uses: ./boomtick-pkg/workflows/prune-stale-previews.yml
    secrets: inherit
