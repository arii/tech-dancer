name: Update Submodule

on:
  repository_dispatch:
    types: [update-submodule]
  workflow_dispatch:
    inputs:
      ref:
        description: 'Branch name, commit hash, or tag to update the submodule to'
        required: true
        default: 'main'
        type: string
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # nosemgrep: yaml.github-actions.security.github-actions-mutable-action-tag.github-actions-mutable-action-tag
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4 # nosemgrep
        with:
          node-version-file: '.node-version'

      - name: Update submodules
        env:
          INPUT_REF: ${{ github.event.inputs.ref }}
        run: |
          set -eo pipefail

          TARGET_REF="${INPUT_REF:-main}"
          echo "Updating boomtick-pkg to ref: $TARGET_REF"

          cd boomtick-pkg
          git fetch origin
          git checkout "$TARGET_REF"
          cd ..

          if [[ -n $(git status --porcelain boomtick-pkg) ]]; then
            # Install dependencies to update lockfile
            corepack enable
            corepack prepare pnpm@10.28.2 --activate
            pnpm install --no-frozen-lockfile
          else
            echo "No changes in submodules."
          fi
        shell: bash

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7 # nosemgrep: yaml.github-actions.security.github-actions-mutable-action-tag.github-actions-mutable-action-tag
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: update submodule to ${{ github.event.inputs.ref || 'main' }} and update lockfile"
          branch: "automation/update-submodule"
          delete-branch: true
          title: "chore: update submodule to ${{ github.event.inputs.ref || 'main' }}"
          body: |
            ## Submodule Update
            - Updated `boomtick-pkg` to ref: `${{ github.event.inputs.ref || 'main' }}`
            - Updated `pnpm-lock.yaml`
            - Automated update triggered by ${{ github.event_name }}
