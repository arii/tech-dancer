name: Auto-Resolve Merge Conflicts

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  call-workflow:
    uses: ./boomtick-pkg/workflows/auto-conflict-resolver.yml
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
      actions: write
