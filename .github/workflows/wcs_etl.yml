name: WCS Data Pipeline - Wednesday Build

on:
  schedule:
    - cron: '0 10 * * 3'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  wcs-etl:
    uses: ./boomtick-pkg/workflows/wcs-etl-reusable.yml
    secrets: inherit # nosemgrep: yaml.github-actions.security.secrets-inherit.secrets-inherit
