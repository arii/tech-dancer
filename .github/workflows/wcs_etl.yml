name: WCS Data Pipeline - Wednesday Build

on:
  schedule:
    - cron: '0 10 * * 3'
  workflow_dispatch:

jobs:
  call-workflow:
    uses: ./boomtick-pkg/workflows/wcs_etl.yml
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
