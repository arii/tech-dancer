{
  "installCommand": "corepack enable && corepack prepare pnpm@10.28.2 --activate && ( [ \"$SKIP_BOOMTICK_PKG\" = \"true\" ] || ( pnpm run doctor && pip3 install -e boomtick-pkg/cli/ --break-system-packages ) ) && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "framework": "vite"
}
