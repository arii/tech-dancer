# Merge Strategy

- First merge the dependency PRs if tests pass.
- Then merge the isolated refactoring and cleanup PRs.
- Finally merge the new feature PRs and ensure CI/CD is fully functional post-merge.
- Keep aggregated PRs under observation.
