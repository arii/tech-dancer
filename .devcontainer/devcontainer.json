{
  "name": "BoomTick Dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "corepack enable && corepack prepare pnpm@10.28.2 --activate && pnpm doctor && pnpm install --frozen-lockfile",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "workspaceFolder": "/workspace",
  "remoteUser": "node"
}
