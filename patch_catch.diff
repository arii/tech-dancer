--- tests/mailing-list.spec.ts
+++ tests/mailing-list.spec.ts
@@ -31,7 +31,7 @@
                 try {
                     const parsedUrl = new URL(url);
                     isGoogleScript = parsedUrl.hostname === 'script.google.com' || parsedUrl.hostname.endsWith('.script.google.com');
-                } catch (e) {
+                } catch {
                     // ignore invalid URL
                 }
